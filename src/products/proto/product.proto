syntax = "proto3";

package product;

service ProductService {
    rpc Create() returns (ProductResponse) {}
}

message ProductRequest {
    string name = 1;
    doable price = 2; 
}

message ProductResponse {
    int32 id = 1;
    string name = 2;
    doable price = 3; 
}