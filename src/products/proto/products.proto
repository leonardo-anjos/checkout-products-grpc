syntax = "proto3";

package products;

service ProductsService {
    rpc Create() returns (ProductsResponse) {}
}

message ProductsRequest {
    string name = 1;
    doable price = 2; 
}

message ProductsResponse {
    int32 id = 1;
    string name = 2;
    doable price = 3; 
}